server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/book_repo
    username: manumafe
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        "[format_sql]": true
    show_sql: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /docs
